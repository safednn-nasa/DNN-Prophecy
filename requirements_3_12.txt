# Core scientific stack (Py3.12 wheels available)
numpy>=1.26.4,<2.0
pandas>=2.1,<2.4
scipy>=1.12,<1.14
scikit-learn>=1.4,<1.6

# TensorFlow on Py3.12
tensorflow==2.17.*   # brings a compatible Keras; do NOT pin keras separately

# Utilities / build
tqdm>=4.66,<5
pybind11>=2.12,<2.13
cmake>=3.25

# ONNX ecosystem (Py3.12 wheels available)
onnx>=1.16,<1.18
onnxruntime>=1.19,<1.22

# Optional / cautious:
# onnx-tf  -> often lags TensorFlow; likely incompatible with TF 2.17, so omit unless you must use it.
# maraboupy -> frequently fails on Colab/Py3.12; usually needs a source build. Omit in Colab.
